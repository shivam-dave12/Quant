# requirements.txt — Unified Dual-Exchange Quant Bot


requests>=2.31,<3
python-dotenv>=1.0,<2
websocket-client>=1.7,<2

python-socketio[client]>=5.11,<6
cryptography>=42,<47
pydantic>=2.7,<3



# HTTP + auth
requests>=2.31.0
cryptography>=41.0.0      # ED25519 for CoinSwitch signature
python-dotenv>=1.0.0

# WebSocket
websocket-client>=1.7.0   # Delta Exchange (raw WS)
python-socketio>=5.10.0   # CoinSwitch (Socket.IO)
websockets>=12.0           # underlying async support

# Telegram
# (uses requests directly — no additional lib needed)

# Utilities
# All standard-library: threading, collections, dataclasses, enum, abc
